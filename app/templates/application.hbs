<div class="container">
  <div class="menu">
    <div class="links">
      {{#link-to "sprites"}}
        sprites
      {{/link-to}}
      {{#link-to "animated-if"}}
        expandable
      {{/link-to}}
      {{#link-to "shapes"}}
        route trans
      {{/link-to}}
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>